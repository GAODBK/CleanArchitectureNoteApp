<resources>

    <style name="Widget.车票.AppWidget.Container" parent="android:Widget">
        <item name="android:id">@android:id/background</item>
        <item name="android:background">?android:attr/colorBackground</item>
    </style>

    <style name="Widget.车票.AppWidget.InnerView" parent="android:Widget">
        <item name="android:background">?android:attr/colorBackground</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <!-- 修改背景颜色 -->
        <item name="android:background">@drawable/dialog_background</item>
        <!-- 修改标题颜色 -->
        <item name="android:textColorPrimary">#FF4081</item>
        <!-- 修改内容颜色 -->
        <item name="android:textColor">#333333</item>
        <!-- 修改按钮颜色 -->
        <item name="android:buttonBarPositiveButtonStyle">@style/CustomDialogButton</item>
    </style>

    <style name="CustomDialogButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">#FF4081</item>
    </style>

</resources>